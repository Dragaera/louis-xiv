%h1 IFTT Maker Keys

%table.table
  %thead
    %tr
      %th Key
      %th Last Used
      %th

  %tbody
    - @maker_keys.each do |key|
      %tr
        %td= link_to key.key, url(:maker_keys, :show, id: key.id)
        %td= key.used_at ? pretty_print_date(key.used_at, true) : '(never)'
        %td
          %a{ href: url(:maker_keys, :edit, id: key.id) }
            %span.glyphicon.glyphicon-edit
